venv/
__pycache__/
*.pyc
.DS_Store
.streamlit/secrets.toml

# Local data copy — production reads the Google Sheet, not this file.
data/*.xlsx

# Raw portfolio drop — the committed portfolio_snapshot.csv is the fallback.
25v26 data set all str.xlsx

# Source-of-truth review report snapshots (reference only, not app data).
26_27_GROWTH DEGROWTH _SHEET*.md
to develop.xlsx
*.py.bak

# Client reference reports — never commit.
reference_*.pdf

# Locally rendered report PDFs — build output, not source (~20 MB per run).
_pdf_*/

# Sample store reports for the extraction work — client data, never commit.
sample_*.pdf

# The full store master carries GST, phone numbers, addresses and store
# mail IDs. This repo is PUBLIC — only the carpet-area extract is tracked.
store_master_full.csv

# Scrapped work kept only for reference — Reports Sonu, retired 19 Aug 2026
# when its one useful report shipped as the DB REPORTS tab. Never pushed.
_archive/
